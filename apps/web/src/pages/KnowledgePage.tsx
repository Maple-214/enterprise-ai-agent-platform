import { FileText, UploadCloud } from 'lucide-react';
import { useQuery, useQueryClient } from '@tanstack/react-query';
import { listDocuments, uploadDocument } from '../lib/api';
export function KnowledgePage(){ const client=useQueryClient(); const query=useQuery({queryKey:['documents'],queryFn:listDocuments}); async function onUpload(file?:File){ if(!file)return; await uploadDocument(file); await client.invalidateQueries({queryKey:['documents']}); } return <div className="page"><div className="page-header"><div><h1>知识库</h1><p>上传企业文档并进入 RAG 检索链路。</p></div><label className="primary-btn upload-btn"><UploadCloud size={17}/>上传文档<input type="file" hidden accept=".txt,.md,.markdown,.csv,.pdf" onChange={e=>void onUpload(e.target.files?.[0])}/></label></div><div className="panel"><div className="panel-title"><h2>文档列表</h2><span>{query.data?.length ?? 0} 个文件</span></div>{query.data?.length?<div className="table">{query.data.map(doc=><div className="table-row" key={doc.id}><div className="file-name"><FileText size={17}/>{doc.filename}</div><span className={`status-pill ${doc.status==='ready'?'success':'warning'}`}>{doc.status==='ready'?'已就绪':doc.status==='queued'?'排队中':doc.status}</span><span className="muted">{new Date(doc.created_at).toLocaleString()}</span></div>)}</div>:<div className="empty">暂无文档。上传 Markdown、TXT、CSV 或 PDF 开始构建知识库。</div>}</div></div> }
